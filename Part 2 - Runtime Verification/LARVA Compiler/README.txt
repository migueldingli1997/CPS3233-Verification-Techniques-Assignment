This is the Larva compiler used for this assignment.

The Larva script can be compiled by running either the
Compile.bat script, which asks for a script name and an
output folder, or the "Compile (Script.lrv).bat" script,
which respectively defaults to Script.lrv and out/.